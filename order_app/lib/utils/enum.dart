enum EFieldType {
  text,
  password,
  phone,
  email,
}

enum EToast {
  success,
  danger,
  warning,
}

enum EColor {
  success,
  primary,
  danger,
  dark,
  secondary,
}

enum EBorder {
  all,
  underline,
}
